# Readme

wallpaper goes into ~/.wallpapers/image.jpg this can be changed in ./programs/hyprland/hyprpaper.nix

alliases: 
- flakeupdate - pulls and updates nix pkgs, home-manager and nvim config 
- nixupdate - sudo nixos-rebuild switch --flake ~/.dotfiles
- homeupdate - home-manager switch --flake ~/.dotfiles

